# anchor-bankrun@0.5.0 still declares its peer as @coral-xyz/anchor@^0.30, which
# excludes 0.31 — but it works fine with 0.31. Accept the stale peer range so
# `npm install` succeeds without needing the --legacy-peer-deps flag.
legacy-peer-deps=true
